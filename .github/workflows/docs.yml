name: Documentation

# Goal 5 of the documentation programme: every site is actually
# reachable. Until this workflow existed magelib rendered to nobody -
# there was no site config at all, and in the two code repos the output
# was gitignored with no workflow building it, which is the same outcome
# reached by a different route.
#
# The build job runs on pull requests too, and deliberately: a site that
# only builds on main tells you it is broken after the merge. Deploy is
# gated to main, so a pull request proves the render without publishing
# it.
#
# TAGS DO NOT BUILD OR DEPLOY, and the reason is a property of Pages
# rather than a preference: an actions/deploy-pages deployment REPLACES
# the whole site. A tag cannot add itself alongside what is published; it
# can only overwrite it, and the next push to main overwrites it back.
# So a tag deploy is not versioning - it is a race with no winner.
#
# v0.8.0 demonstrated it. The tag pointed at main's own tip, so the
# deploy would have republished byte-identical content, and it failed
# anyway because the github-pages environment allow-lists main. The
# environment refusing it is the fail-closed layer working.
#
# Versioned documentation, when it is wanted, belongs in THIS job: the
# main build enumerates the released tags, renders each into its own
# subdirectory, and deploys the assembled tree as ONE artifact. That
# keeps the site a pure function of the repository, with no accumulated
# state and no second deployment to race.

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

# Pages allows one concurrent deployment. cancel-in-progress is false
# rather than true: cancelling a half-finished deployment leaves the
# published site in whatever state it reached, which is worse than
# queueing behind it.
concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    name: Render the Site
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Synchronize Environment (Nix)
        uses: cachix/install-nix-action@v24
        with:
          nix_path: nixpkgs=channel:nixos-unstable

      # OPERATOR DECISION 65: CACHE BEFORE GATING. The relearn theme is
      # a Hugo module, and it is 43M extracted.
      #
      # THE PATH IS HUGO'S OWN CACHE AND NOT $GOMODCACHE, WHICH IS THE
      # ASSUMPTION THIS STEP WAS FIRST WRITTEN AGAINST. Hugo drives Go
      # modules but keeps its own store: measured 2026-08-07 with a fully
      # warm $GOMODCACHE, a render still wrote 71M into
      # hugo_cache/modules/filecache/modules/pkg/mod, a second copy of
      # the same theme. Caching the Go module cache instead would have
      # been a cache that never hits, which is worse than none - it pays
      # the upload and keeps the fetch.
      #
      # The key is docs/go.sum, which is COMMITTED here and pins the
      # theme's exact pseudo-version, so a theme bump misses by
      # construction rather than serving a stale tree. The restore-keys
      # prefix makes that miss partial instead of total.
      #
      # THIS JOB IS NOT THE CONSUMER THE CACHE EXISTS FOR.
      # MAGELIB-DIV-012's h1 gate renders the site inside `mage lint`,
      # which would hand a pull-request gate a 43M network dependency on
      # a cold cache. Caches restore from the default branch and are
      # scoped to the repository rather than to the workflow, so this job
      # populating the key on main is what ci.yml will restore later.
      # That ordering is the decision; the gate is a separate change.
      - name: Cache the Hugo Module
        uses: actions/cache@v4
        with:
          path: ~/.cache/hugo_cache
          key: hugo-modules-${{ runner.os }}-${{ hashFiles('docs/go.sum') }}
          restore-keys: |
            hugo-modules-${{ runner.os }}-

      # docs:build syncs the shared assets, regenerates the API
      # reference, and renders. It does NOT gate the result: the drift
      # check is part of `mage lint` in the CI workflow, because a stale
      # reference is a lint failure on the pull request rather than a
      # deploy failure after merge.
      - name: Build Documentation
        run: nix develop -c mage -v docs:build

      - name: Upload Artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: docs/public

  deploy:
    name: Publish to Pages
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    # Kept as an explicit guard even though the trigger above no longer
    # fires on tags. Two independent conditions have to be wrong before
    # a non-main ref can publish, and the trigger is the one a later
    # change is likely to widen without thinking about this job.
    if: github.ref == 'refs/heads/main'
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
